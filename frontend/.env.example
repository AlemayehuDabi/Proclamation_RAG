# Public API base (no trailing slash). Production: your deployed FastAPI origin.
# VITE_API_BASE_URL=https://api.example.com

# Development: defaults to /api with Vite proxy → FastAPI
VITE_API_BASE_URL=/api

# Where Vite proxies /api in dev (optional)
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
