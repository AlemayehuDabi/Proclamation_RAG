# Dev: browser calls same-origin `/api/*`; Vite proxies to FastAPI (see vite.config.ts).
VITE_API_BASE_URL=/api
# Override backend if not on 127.0.0.1:8000
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
